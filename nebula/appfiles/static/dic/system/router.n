
.* #{
访问路径:%:%括号0%
路径尾巴:$:$取后字符 1 %访问路径%$
404文件::public/404.wn
默认文件:index.wn

如果:%路径尾巴%==/
    访问路径+:%默认文件%
如果尾

存在:$:$存在文件 public%访问路径%$
文件后缀:$:$文件后缀 %访问路径%$

如果:%存在%==false
    $全局变量 响应状态 404$
    $设置头部 Content-Type text/html;charset=UTF-8$
    $执行网页词库文件 %404文件%$

// Nebula
否则如果:%文件后缀%==.n
    $执行词库文件 public%访问路径% Main$

// php win端
/*
否则如果:%文件后缀%==.php
    文件数据:$:$读文件 public%访问路径% %404默认值%$
    $PHP %文件数据% %_请求数据_% %_响应数据_%$
*/

// WebNebula
否则如果:%文件后缀%==.wn
    $设置头部 Content-Type text/html;charset=UTF-8$
    $执行网页词库文件 public%访问路径%$

否则如果:%文件后缀%==.md
    $设置头部 Content-Type text/html;charset=UTF-8$
    文件数据:$:$读文件 public%访问路径%$
    <!DOCTYPE html><html lang="en"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1"><link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/github-markdown-css/4.0.0/github-markdown.min.css"></head><body>
    <article class="markdown-body">$MD转HTML %文件数据%$</article></body></html>

否则如果:%文件后缀%==.txt
$读文件 public/%访问路径%$

否则

MIME:$MIME类型 %文件后缀%$
$设置头部 Content-Type %MIME%$
$设置头部 Accept-Ranges bytes$
$读文件 public%访问路径%$
}#